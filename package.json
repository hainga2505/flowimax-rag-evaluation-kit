{
  "name": "flowimax-rag-evaluation-kit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A reproducible, source-grounded RAG evaluation portfolio project using only synthetic data.",
  "scripts": {
    "eval": "node src/run-evaluation.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify:fixtures": "node scripts/verify-fixtures.mjs",
    "security:audit": "node scripts/prepublish-audit.mjs",
    "verify": "npm run verify:fixtures && npm run security:audit && npm test && npm run eval && node scripts/verify-report.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
